{
  "name": "umbraforge",
  "version": "0.12.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4177",
    "play": "node server.cjs",
    "test": "node --test tools/*.test.js",
    "validate:java": "javac -d tools/.build tools/CampaignValidator.java && java -cp tools/.build CampaignValidator",
    "audit:python": "python tools/asset_audit.py",
    "verify": "npm run test && npm run build && npm run validate:java && npm run audit:python"
  },
  "dependencies": {
    "phaser": "3.90.0",
    "three": "^0.186.0"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
